# دليل تشغيل موقع "قوائم تعبئة صبراتة" — موقع حقيقي مستقل ومجاني

هذا الدليل يشرح خطوة بخطوة كيف تُشغّلون هذا الموقع كموقع حقيقي مستقل، بدون أي علاقة بـ Claude، وبدون تسجيل دخول من الزوار، باستخدام حسابيكم المجانيين في **Google** (لقاعدة البيانات Firebase) و**GitHub** (لاستضافة الموقع).

الوقت المتوقع: حوالي 15-20 دقيقة، وكلها خطوات نقر بدون أي برمجة.

---

## المزايا في هذا الإصدار

بالإضافة للتسجيل والاستدعاء الأساسي، يتضمن الموقع الآن:

- **شارة "بيانات لحظية مباشرة"** أعلى الصفحة، تؤكد للزوار أن الأرقام حقيقية ومحدّثة.
- **إحصاءات مباشرة على كل محطة**: عدد المنتظرين الآن، عدد من نودي عليهم، وإجمالي تراكمي (منذ إنشاء المحطة) لمن تمت تعبئتهم ولعابري السبيل — يظهر هذا لكل من مديرية الأمن وصاحب المحطة، بدون أي تعديل من جهة صاحب المحطة.
- **لوحة تقارير شاملة** (زر "📊 لوحة التقارير" داخل لوحة مديرية الأمن): جدول واحد يجمع كل المحطات مع كل الأرقام، ويبقى صحيحاً حتى بعد استخدام زر "مسح المكتمل والملغى" لأن الإجماليات تُحسب بشكل تراكمي منفصل عن قائمة الانتظار الحالية.
- **زر طباعة التقرير**: يطبع الجدول بشكل نظيف بدون أزرار أو عناصر واجهة، مناسب لأرشفة اللجنة اليومية أو الأسبوعية.

---

## الفكرة العامة

الموقع يتكوّن من ثلاثة أجزاء يجب ربطها ببعضها مرة واحدة فقط:

1. **قاعدة بيانات Firebase** — تخزّن المحطات وقوائم الانتظار وتُحدّث كل الزوار لحظياً.
2. **ملف الموقع `index.html`** — الواجهة التي يراها الناس، ستُدخلون فيه بيانات مشروع Firebase الخاص بكم.
3. **استضافة GitHub Pages** — تنشر الملف كموقع فعلي بعنوان دائم يفتح للجميع بدون تسجيل دخول.

---

## الجزء الأول: إنشاء قاعدة بيانات Firebase (مجانية)

1. افتحوا [console.firebase.google.com](https://console.firebase.google.com) وسجّلوا الدخول بحساب Google الذي تريدون استخدامه.
2. اضغطوا **Add project / إضافة مشروع**، واكتبوا اسماً مثل `sabratha-fuel-queue`، ثم تابعوا الخطوات (يمكن تعطيل Google Analytics، غير ضروري هنا) حتى ينشئ المشروع.
3. من القائمة الجانبية اليسرى، افتحوا **Build ← Firestore Database**.
4. اضغطوا **Create database**.
   - اختاروا موقع الخادم (Location) الأقرب جغرافياً (مثلاً `eur3` لأوروبا أو أي خيار قريب من ليبيا).
   - اختاروا **Start in production mode** (وضع الإنتاج) — هذا مهم.
5. بعد إنشاء القاعدة، افتحوا تبويب **Rules** أعلى الصفحة.
6. افتحوا الملف المرفق `firestore.rules` من هذه الحزمة، انسخوا كل محتواه، والصقوه في المكان بدلاً من القواعد الافتراضية.
7. اضغطوا **Publish**.

> ملاحظة: ملف `firestore.rules` يشرح داخل تعليقاته حدود هذا النظام الأمني — يُنصح بقراءتها.

### تسجيل تطبيق ويب للحصول على بيانات الربط (Config)

1. ارجعوا لصفحة إعدادات المشروع: أيقونة الترس ⚙️ أعلى يسار القائمة الجانبية ← **Project settings**.
2. انزلوا لأسفل حتى قسم **Your apps**، واضغطوا أيقونة الويب `</>`.
3. اكتبوا اسماً للتطبيق (مثلاً `sabratha-web`) واضغطوا **Register app** — لا حاجة لتفعيل Firebase Hosting هنا.
4. ستظهر لكم قطعة كود فيها كائن `firebaseConfig` يشبه هذا:

```js
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "sabratha-fuel-queue.firebaseapp.com",
  projectId: "sabratha-fuel-queue",
  storageBucket: "sabratha-fuel-queue.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef123456"
};
```

احتفظوا بهذه القيم الست — ستحتاجونها في الخطوة التالية مباشرة.

---

## الجزء الثاني: تعبئة بيانات الربط داخل ملف الموقع

1. افتحوا ملف `index.html` من هذه الحزمة بأي محرر نصوص (حتى Notepad أو TextEdit يكفي).
2. ابحثوا عن هذا الجزء قرب نهاية الملف:

```js
const FIREBASE_CONFIG = {
  apiKey: "ضع-قيمتك-هنا",
  authDomain: "ضع-قيمتك-هنا",
  projectId: "ضع-قيمتك-هنا",
  storageBucket: "ضع-قيمتك-هنا",
  messagingSenderId: "ضع-قيمتك-هنا",
  appId: "ضع-قيمتك-هنا"
};
```

3. استبدلوا كل `"ضع-قيمتك-هنا"` بالقيمة المقابلة لها التي نسختموها من Firebase (مع الحفاظ على علامتي التنصيص `" "`).
4. **مهم جداً:** بجانبها مباشرة يوجد هذا السطر:

```js
const SECURITY_PIN = "صبراتة2026";
```

غيّروا `"صبراتة2026"` إلى رمز سري من اختياركم أنتم فقط — هذا هو الرمز الذي سيدخل به أعضاء اللجنة إلى لوحة مديرية الأمن، فلا تتركوه كما هو ولا تشاركوه إلا مع أعضاء موثوقين.

5. احفظوا الملف.

---

## الجزء الثالث: نشر الموقع مجاناً عبر GitHub Pages

1. افتحوا [github.com](https://github.com) وسجّلوا الدخول.
2. اضغطوا **+** أعلى يمين الصفحة ← **New repository**.
   - اسم المستودع: مثلاً `sabratha-fuel-queue`.
   - اجعلوه **Public**.
   - لا تضيفوا README أو أي ملف الآن، اتركوه فارغاً، ثم اضغطوا **Create repository**.
3. في صفحة المستودع الفارغ، اضغطوا رابط **uploading an existing file**.
4. اسحبوا ملف `index.html` (بعد تعديله في الجزء الثاني) وأفلتوه في الصفحة، ثم اضغطوا **Commit changes** أسفل الصفحة.
   - ملفا `firestore.rules` و`SETUP.md` لا يحتاجان الرفع هنا — هما فقط للرجوع إليهما، ويمكن رفعهما أيضاً إن أردتم أرشفتهما مع المشروع.
5. من إعدادات المستودع: تبويب **Settings** ← من القائمة الجانبية **Pages**.
6. تحت **Build and deployment ← Source** اختاروا **Deploy from a branch**.
7. تحت **Branch** اختاروا `main` والمجلد `/ (root)`، ثم اضغطوا **Save**.
8. انتظروا دقيقة أو دقيقتين، ثم أعيدوا فتح صفحة **Pages** نفسها — سيظهر أعلاها رابط بالشكل:

```
https://<اسم-حسابكم>.github.io/sabratha-fuel-queue/
```

هذا هو **رابط الموقع العام** النهائي — يعمل لأي زائر مباشرة بدون أي تسجيل دخول أو علاقة بـ Claude.

---

## روابط اللوحات الثلاث

بعد نشر الموقع، الروابط الثلاثة هي:

| اللوحة | الرابط | من يستخدمها |
|---|---|---|
| **الموقع العام** (تسجيل الدور) | `https://<اسم-حسابكم>.github.io/sabratha-fuel-queue/` | كل السائقين |
| **لوحة مديرية الأمن** (تحكم كامل) | نفس الرابط أعلاه + `#security` | أعضاء اللجنة فقط، بعد إدخال الرمز |
| **لوحة صاحب المحطة** (عرض فقط) | تُنسخ تلقائياً من داخل لوحة الأمن (زر "نسخ رابط صاحب المحطة" في كل محطة) | صاحب تلك المحطة فقط |

شاركوا رابط `#security` فقط مع اللجنة، ولا تنشروه في مجموعات الواتساب العامة.

---

## تحديث الموقع لاحقاً

أي تعديل مستقبلي (تغيير الرمز السري، تعديل نص، إلخ) يكون بنفس الطريقة: عدّلوا `index.html` محلياً، ثم من صفحة المستودع على GitHub اضغطوا على الملف ← أيقونة القلم للتعديل (أو ارفعوه من جديد ليستبدل القديم) ← Commit changes. يتحدث الموقع تلقائياً خلال دقيقة أو دقيقتين.

---

## أسئلة متوقعة

**هل هذا مجاني فعلاً وبشكل دائم؟**
نعم، ضمن الحدود المجانية لكل من Firebase (خطة Spark) وGitHub Pages، وهي حدود سخية جداً (عشرات الآلاف من القراءات يومياً) وتكفي بسهولة لحجم استخدام مدينة مثل صبراتة.

**ماذا لو ظهرت رسالة "تعذّر الاتصال بقاعدة البيانات"؟**
تأكدوا أن قيم `FIREBASE_CONFIG` صحيحة ومطابقة تماماً لما في Firebase Console، وأن قواعد Firestore تم نشرها (زر Publish) في الجزء الأول.

**هل يمكن ربط اسم نطاق خاص بكم (مثل sabratha-fuel.ly) بدلاً من رابط github.io؟**
نعم، GitHub Pages يدعم ذلك مجاناً من تبويب Settings ← Pages ← Custom domain، إن توفر لديكم نطاق مسجّل مسبقاً.
